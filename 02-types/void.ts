(() => {

    //* Void
    // El valor de void significa vacio
    // es decir, no hay nada en ese valor
    function callMe (): void {};

    const callMeV = ():void => {};

    const voidV = callMe();
    console.log( voidV );

})()