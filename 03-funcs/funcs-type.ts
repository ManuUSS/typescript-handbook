(() => {

    //* Funciones

    const addNumber = ( numA: number, numB: number): number => numA + numB;
    const greet = ( name:string ): string => `Hola ${ name }`;
    const saveTheWorld = (): string => `El mundo está a salvo`;



})()